---
import { getEntry } from "astro:content";

import Layout from "../layouts/Layout.astro";

const post = await getEntry("other", "index");

const { Content } = await post.render();
---

<Layout slug={post.slug}>
  <Content />
</Layout>
