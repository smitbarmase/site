---
import { getEntry } from "astro:content";

import Layout from "../layouts/Layout.astro";

const post = await getEntry("other", "404");

const { Content } = await post.render();
---

<Layout slug={post.slug}>
  <Content />
</Layout>
